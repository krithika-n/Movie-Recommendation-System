Readme
--------------------------------------

All the commands to execute are given in section 2 of report.

An existing database cannot be used because of the column names difference. Hence, for the programs to work, a database should be created. 
This can be done by using the appropriate command mentioned in section 2 of report.

The user used is postgres. For the password , please change it in top section of tasks.py

The entire project structure must be kept same as it was extracted, since the loading script uses the location mentioned in the script.
the datasets in the phase1_dataset folder must be kept same as it is. The csv can be changed but the column names must be removed and the file names must be same.

All the tasks can be perfomed as per the commands mentioned in the section 2 of the report.

The output.txt file will contain the input given and the output from each of the tasks.



